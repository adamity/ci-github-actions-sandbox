name: Staging
run-name: Run CI/CD Cycle for Staging Environment
on:
  push:
    branches:
      - develop
jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - run: echo "This job is triggered by a push to the develop branch"
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Setup .env file
        run: echo "CI_ENVIRONMENT=development" >> .env && echo "app.baseURL='http://example.com'" >> .env
      - name: Show .env file
        run: cat .env
      - name: Set permissions for logs directory recursively to avoid permission errors
        run: chmod -R 777 ${{ github.workspace }}/writable/logs
      - name: Install dependencies
        run: composer install
      - name: Run PHPUnit tests
        run: ${{ github.workspace }}/vendor/bin/phpunit
  deploy:
    name: Deploy to Staging
    steps:
      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: unnecessary
      - name: Adding Known Hosts
        run: ssh-keyscan -p ${{ secrets.SSH_PORT}} -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
      - name: Update Staging by pulling from GitHub
        run: ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cd ${{ secrets.SSH_PATH }} && git pull"
        # run: rsync -avz -e "ssh -o StrictHostKeyChecking=no" --delete ${{ github.workspace }}/ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_PATH }}
        # run: rsync -avz -e "ssh -p ${{ secrets.SSH_PORT }}" --delete ${{ github.workspace }}/ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_PATH }}
